# SFT
## 为什么需要SFT
通过大量语料，逐个token的训练模型可以获得一个通用模型，这个阶段是pre train，它将知识和能力内化了，但是还需要教会模型怎么沟通。比如让它按照指定的格式输出问题，或者让它能友好的沟通等，需要在这一步完成，称为post train或者fine tune。

## 怎么做SFT
监督微调（Supervised Fine-Tuning, SFT）是最自然的想法，延续了pre train的策略，人工筛选出特定语料或者编写特定语料，以这种有监督的方式，像预训练阶段一样让它逐token的学习。比如，想让它学会思维链、以json的格式输出等，都可以使用这种方法。

## 缺陷是什么
1. 高度依赖人工标注数据，需要大量高质量、多样化的任务相关标注数据，获取成本高且容易受标注者偏见影响。如果标注数据中存在噪声、不一致或错误，SFT 会直接学习这些错误，导致模型输出不可靠。错误纠正通常需要重新标注或数据清洗，成本较高。
2. 泛化能力有限，模型实际上还是在“学说话”。如果时间、领域、用户群体的变化，模型性能会显著下降，持续更新数据或重新微调才能维持效果。
3. 灾难性遗忘，在按照目标做大量微调时，可能会过度适应新任务而遗忘预训练阶段学到的通用知识。
4. 输出多样性不足，由于训练目标是最大化给定输入的标注输出概率，模型容易产生固定、模板化的回复，缺乏创造性和多样性。对于需要开放生成的任务（如创意写作、对话），这可能限制表现。

## 实验结果
训练参数设置如下：
```python
config = {
        "n_sft_steps": 200,
        "learning_rate": 1e-5,
        "train_batch_size": 256,
        "gradient_accumulation_steps": 128,
    }
```

使用data中的sft数据集训练200步，根据梯度累积方法拆分总256的batch size，microbatch为2。step_loss如下图，稳定收敛。

![Alt text](https://cdn4.winhlb.com/2026/05/26/6a1505afa2f44.png)


# GRPO
## 为什么需要强化学习
1. 强化学习缓解了数据标注的需求。
2. 在SFT中，训练时每个token的输入都是真实的前缀（teacher forcing）；但推理时模型看到的是自己生成的token。这种不一致叫暴露偏差，会导致错误累积。RL在训练时就是自己生成完整序列，然后根据奖励信号更新模型。这更贴近推理时的真实分布，因此可以缓解暴露偏差，提升生成质量。
3. 可以灵活引入复杂的、不可微的目标，人类的偏好往往包括多个维度（有用性、安全性、趣味性等），很难写成简单的损失函数。奖励模型本质上是一个学习的黑盒打分器，它的梯度无法直接传给生成模型（因为生成是离散的采样过程）。  强化学习（如策略梯度方法）不要求奖励函数可微，只要求能获得奖励值，就可以通过诸如REINFORCE或PPO的算法更新模型。

## 怎么做
对于数学或代码这种具有明确评估标准的任务，强化学习方法非常合适。在强化学习中将llm作为policy，按照policy采样每一个文本作为action，一条完整的文本序列作为rollout。

它会用旧模型针对同一个问题采样多条轨迹，把这几条轨迹作为一个（group），然后计算相对的优势值（advantage），即各条轨迹获得的奖励与组内平均值相比如何，从而决定要强化还是抑制这条轨迹的概率。

### “损失”函数
GRPO方法可以追溯到策略梯度（Policy Gradient），其目标是寻找一个最优策略，最大化这个策略在环境中的回报，将目标函数定义为$J(\theta) = E_{s_0}[V^{\pi_{\theta}}(s0)]$，然后通过用目标函数对策略参数求导，获得梯度后采用梯度上升方法求得最优策略。因此在GRPO中并不真的定义损失函数，知识借用自动微分机制进行梯度更新。最终GRPO的目标函数为：
$$J_{\text{GRPO-Clip}}(\theta) = \mathbb{E}_{q \sim \mathcal{D},\; \{o^{(i)}\}_{i=1}^{G} \sim \pi_{\theta}(\cdot \mid q)} \left[ \frac{1}{G} \sum_{i=1}^{G} \frac{1}{|o^{(i)}|} \sum_{t=1}^{|o^{(i)}|} \min \left( \frac{\pi_{\theta}(o^{(i)}_t \mid q, o^{(i)}_{<t})}{\pi_{\theta_{\text{old}}}(o^{(i)}_t \mid q, o^{(i)}_{<t})} A^{(i)},\; \operatorname{clip}\!\left( \frac{\pi_{\theta}(o^{(i)}_t \mid q, o^{(i)}_{<t})}{\pi_{\theta_{\text{old}}}(o^{(i)}_t \mid q, o^{(i)}_{<t})},\; 1-\varepsilon,\; 1+\varepsilon \right) A^{(i)} \right) \right]$$

其中，Clip部分继承自TRPO/PPO，通过重要性采样裁剪比率，保证每次策略更新不破坏之前学到的行为，避免一步跨太大导致性能崩溃。

### 样本效率
训练所用的数据全来自于当前policy的方法称为on-policy，这种方法的好处是每次训练都用自己生成的数据，梯度估计是无偏的，稳定性好。但数据用完即弃，不能重复利用，样本效率低。因此也可以使用off-policy方法，即一次生成的数据训练好几轮，后续的轮次所用的数据就不是他们自己生成的了，好处是样本效率高，但需要用重要性采样来修正数据分布差异，可能带来带来的高方差和数值不稳定问题，要引入比率裁剪稳定训练。

### 梯度累积
当batch size比较大，一次无法塞入一个batch size的数据时，可以采用梯度累积的方法，具体来说，就是将batch 分为小的micro batch，每次只使用micro batch的数据，然后在loss中除以（micro batch size/batch size），决定这一次的梯度占总梯度的比例。在使用完一个batch的数据后，才进行一次更新，中间不清除梯度。

那为什么不只对loss做累积，在最后统一backward呢？原因每次loss决定的是模型下一步往哪儿走的参考，还与输入参数有关，真正重要的是梯度，决定模型参数下一步往哪儿走。所以传递loss和传递梯度具有本质的不同，再直观一点，loss是一个标量，而梯度是高维的张量，传递loss相当于用一个标量压缩了一个高维张量的信息，只在最后一步做backward没办法还原中间每一步的梯度方向。


## 缺陷是什么
1. 对组内样本质量敏感，如果组内样本差异很小（例如奖励模型无法区分优劣）或差异剧烈（存在极端低分样本），相对优势的估计可能出现偏差，导致策略更新不稳定。
2. 奖励模型缺陷的放大，如果奖励模型存在系统性偏差（例如对某些模式偏好或对长度敏感），这种偏差会在组内比较中被放大，使模型学会“钻营”奖励模型的弱点而非真正符合人类偏好。
3. 训练稳定性挑战，由于优势估计完全基于组内统计量（如均值、标准差），当组内样本的奖励分布剧烈变化时，优势值的尺度可能突变，导致策略梯度步长不稳定。
4. 若组内所有输出都错误或都正确，组内相对优势为零，无法提供有效的学习信号。
5. 先样本内的损失平均，再样本间的损失平均。这样每个样本的权重是一样的，对于长序列而言并不公平。

## 实验结果



# 参考资料
1. [CS336 Spring 2025 Assignment 5: Alignment (现已更新到2026版)](https://github.com/stanford-cs336/assignment5-alignment)
2. [个人实现仓库（暂未更新至2026版）](https://github.com/MAGMA27/assignment5-alignment)